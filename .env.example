# Set to the actual site origin before building for deployment. A path becomes the basePath.
SITE_URL=http://localhost:3000
# Keep staging out of search results. Set true only for the public launch.
SITE_INDEXABLE=false
